
{-
`lt` checks if one Natural is less than or equal to another.

Examples:

```
./lte 5 6
= True

./lte 5 5
= True

./lte 5 4
= False
```
-}
let lte
	: Natural → Natural → Bool
	= λ(x : Natural) → λ(y : Natural) → Natural/isZero (Natural/subtract y x)

in  lte
